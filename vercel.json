{
  "buildCommand": "cd client && npm install && npm run build && cp client/dist/index.html client/dist/404.html",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ],
  "functions": {
    "api/index.js": {
      "memory": 256,
      "maxDuration": 30
    }
  }
}
